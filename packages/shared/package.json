{
  "name": "@poc/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@poc/tsconfig": "workspace:*",
    "rimraf": "^6.1.3",
    "typescript": "~6.0.3"
  }
}
